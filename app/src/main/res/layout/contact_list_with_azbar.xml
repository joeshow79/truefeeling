<?xml version="1.0" encoding="utf-8"?><!-- if not use framelayout AzBar will call onLayout(false) when soft key board showing -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:sio="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <android.support.v4.widget.SwipeRefreshLayout
        android:id="@+id/swipe_refresh_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ListView
            android:id="@+id/contacts_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:cacheColorHint="@color/white"
            android:listSelector="@drawable/contacts_list_selector"
            android:scrollbars="none" />
    </android.support.v4.widget.SwipeRefreshLayout>
</FrameLayout>
